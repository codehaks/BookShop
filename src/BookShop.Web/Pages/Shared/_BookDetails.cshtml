@model BookData

<p>Name : @Model.Name</p>
<p>Language : @Model.Language</p>
@if (Model.CoverImage is not null) {
    <img src="@("data:image/jpg;base64,"+Convert.ToBase64String(Model.CoverImage))" class="img-fluid" style="max-height:200px" />
}
